name: MineSightFarm
version: '${version}'
main: com.minesight.farm.MineSightFarmPlugin
api-version: '1.21'
folia-supported: true
description: Server-side ore scanning, spectator teleporting, and capture orchestration for the MineSight dataset farm.
authors: [kopec]
# Paper plugins do not support YAML command declarations - the /minesightfarm
# command (alias /msf) is registered in MineSightFarmPlugin#onEnable via
# JavaPlugin#registerCommand.
